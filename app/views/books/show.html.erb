<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3 mt-4">

      <%= render 'layouts/error', model: @new_book%>

      <%= render 'users/user_info', user: @user %>

      <%= render 'books/new_book', book: @new_book %>
    </div>

    <div class="col-md-8 offset-md-1 mt-4">
       <h2>Book detail</h2>
       <table class="table">
         <tbody>
           <tr></tr>
           <tr id="book_<%= @book.id %>">
             <td>
               <%= link_to user_path(@book.user) do %>
                 <%= attachment_image_tag @book.user, :profile_image, :fill, 30, 30, fallback: "no_image.jpg", size: "50x50", class:"mb-2"  %>
                  <br>
                 <%= @book.user.name %>
               <% end %>
             </td>
             <td>
               <%= link_to book_path(@book) do %>
                 <%= @book.title %>
               <% end %>
             </td>
             <td><%= @book.body %></td>
             <td class="favorite-btn-<%= @book.id %>">
               <%= render 'favorites/favorite-btn', book: @book %>
             </td>
             <td class="comments-count">コメント数：<%= @book.book_comments.count %></td>
             <% if current_user.id == @book.user_id %>
               <td>
                 <%= link_to "Edit", edit_book_path(@book), class:"btn btn-success btn-sm" %>
               </td>
               <td>
                 <%= link_to "Destroy", book_path(@book), method: :delete, class:"btn btn-danger btn-sm", "data-confirm" => "本当に消しますか" %>
               </td>
             <% end %>
           </tr>
         </tbody>
       </table>
       <div id="comment_error">

       </div>

       <div class="comment-wrapper">
         <div class="container">
            <h4>Comment</h4>
            <div id="comment_index">
              <%= render 'books/comment',  book_comments: @book_comments %>
            </div>
            <div class="comment_form">
              <%= form_with(model:[@book, @book_comment], remote: true) do |f| %>
                <%= f.text_area :comment, rows:"3", placeholder: "コメントはここに", class:"form-control mb-3" %>

                <%= f.submit "送信", class:"btn btn-sm btn-info ml-auto d-block"%>
              <% end %>
            </div>
          </div>
       </div>
    </div>
  </div>
</div>