<% flash.each do |key, value| %>
  <% if flash[:notice] %>
   <%= content_tag(:div, value, class: "flash text-light bg-info")%>
  <% else %>
   <%= content_tag(:div, value, class: "flash text-light bg-warning")%>
  <% end %>
<% end %>